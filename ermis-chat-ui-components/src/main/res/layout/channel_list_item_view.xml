<?xml version="1.0" encoding="utf-8"?>

<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/channelItemView"
    android:layout_width="match_parent"
    android:layout_height="@dimen/ui_channel_list_item_height"
    >

    <include
        android:id="@+id/itemBackgroundView"
        layout="@layout/channel_list_item_background_view"
        />

    <include
        android:id="@+id/itemForegroundView"
        layout="@layout/channel_list_item_foreground_view"
        />

</FrameLayout>
